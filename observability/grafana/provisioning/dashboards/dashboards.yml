apiVersion: 1

providers:
  - name: ZenOps
    orgId: 1
    folder: ZenOps
    folderUid: zenops
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
