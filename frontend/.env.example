# Example environment variables for the React frontend

# Optional backend API base URL override.
# Leave empty for automatic detection:
# - Vite dev on localhost:5173 -> http://localhost:8000
# - Docker/proxy deployments -> same-origin (/api)
VITE_API_URL=

# Enable debugging messages in the browser console
VITE_DEBUG=true
