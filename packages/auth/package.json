{
  "name": "@zenops/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "contract_check": "echo 'no contract checks for auth'"
  },
  "dependencies": {
    "jsonwebtoken": "9.0.2",
    "zod": "3.24.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "9.0.9",
    "vitest": "3.0.6"
  }
}
